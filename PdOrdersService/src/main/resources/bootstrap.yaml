spring:
  application:
    name: pd-products-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      fail-fast: true
  config:
    import: optional:${CONFIG_SERVER_URL:http://localhost:8888}